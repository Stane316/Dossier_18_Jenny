# Both functions enforce their own authorization/CORS contracts.
# contribution-pipeline is intentionally public but rate-limited; jenny-access
# validates the current Supabase Auth access token in its handler.
[functions.jenny-access]
verify_jwt = false

[functions.contribution-pipeline]
verify_jwt = false
